# TensorFlow Requirements for CXR Foundation Embedding Computation
# Install these ONLY for running compute_cxr_embeddings.py
tensorflow==2.15.0
tensorflow-text==2.15.0

# Hugging Face Hub (for downloading model)
huggingface_hub==0.19.4

# Core dependencies
numpy==1.23.5
pandas==1.5.3
Pillow==9.5.0
tqdm==4.65.0

# Note: After computing embeddings, you can uninstall TensorFlow
# and use the regular requirements.txt for PyTorch training
